(() => {let current=null;const STYLE_ID='rolarp-style';function style(){if(document.getElementById(STYLE_ID))return;const s=document.createElement('style');s.id=STYLE_ID;s.textContent='.rolarp-chip{display:inline-flex!important;align-items:center;gap:4px;padding:2px 7px;border-radius:999px;background:#635bff;color:#fff;font-size:11px;font-weight:700}.rolarp-local{outline:1px dashed #635bff!important}';document.documentElement.appendChild(s)}function replaceText(root,needle,value){if(!needle||needle===value)return;const w=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);const nodes=[];while(w.nextNode())nodes.push(w.currentNode);nodes.forEach(n=>{if(n.nodeValue&&n.nodeValue.trim()===needle)n.nodeValue=value})}function apply(state){current=state;style();const selectors=['[data-testid="robux-balance"]','.text-robux','.robux-balance'];selectors.forEach(sel=>document.querySelectorAll(sel).forEach(el=>{el.textContent=Number(state.robux).toLocaleString();el.classList.add('rolarp-local')}));if(state.username){const body=document.body;replaceText(body,window.__rolarpLastUsername||'',state.username);window.__rolarpLastUsername=state.username}if(state.displayName){replaceText(document.body,window.__rolarpLastDisplayName||'',state.displayName);window.__rolarpLastDisplayName=state.displayName}document.querySelectorAll('[data-testid="premium-badge"]').forEach(el=>{el.style.display=state.premium?'':'none'});document.querySelectorAll('[data-testid="verified-badge"]').forEach(el=>{el.style.display=state.verified?'':'none'})}chrome.runtime.onMessage.addListener(msg=>{if(msg?.type==='ROLARP_APPLY')apply(msg.state)});new MutationObserver(()=>{if(current)apply(current)}).observe(document.documentElement,{subtree:true,childList:true});chrome.storage.local.get('rolarp',x=>{if(x.rolarp)apply(x.rolarp)});})();
